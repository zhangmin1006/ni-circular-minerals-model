# Pinned with upper bounds so a fresh Streamlit Community Cloud build resolves to
# the tested-good majors and a future breaking release cannot silently break the
# deploy. Floors are the minimum the code needs; caps are the next major.
numpy>=2.0,<3
pandas>=2.2,<3.1
mesa>=3.1,<4              # firm-grounded ABM uses the mesa 3.x rng= API
networkx>=3.0,<4         # required by mesa 3.x
scipy>=1.11,<2
matplotlib>=3.8,<4
python-docx>=1.1,<2      # Word export (export_word.py)
streamlit>=1.49,<2       # st.dataframe(width="stretch") needs >= 1.49
